# /etc/systemd/system/xray.service.d/10-donot_touch_single_conf.conf
# Generated by deploy-kit (external/) — DO NOT EDIT BY HAND
# Year:    {{CURRENT_YEAR}}
# Node:    {{NODE_HOSTNAME}}
# RUN_ID:  {{RUN_ID}}
#
# This drop-in mirrors the layout produced by the upstream xray-install
# script. We keep it for forward-compatibility — if someone re-runs the
# upstream installer, the drop-in matches what they expect.

[Service]
ExecStart=
ExecStart={{XRAY_INSTALL_DIR}}/xray run -config {{XRAY_CONFIG_DIR}}/config.json