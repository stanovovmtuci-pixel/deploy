# /etc/ssh/sshd_config.d/00-deploy.conf
# Generated by deploy-kit (external/) — DO NOT EDIT BY HAND
# Year:   {{CURRENT_YEAR}}
# Node:   {{NODE_HOSTNAME}}
# RUN_ID: {{RUN_ID}}
#
# This drop-in overrides /etc/ssh/sshd_config defaults.
# To change values, update manifest.json and re-run phase 02.

# --- Listen ---
Port {{SSHD_PORT}}
AddressFamily any
ListenAddress 0.0.0.0
ListenAddress ::

# --- Authentication ---
PermitRootLogin {{SSHD_PERMIT_ROOT_LOGIN}}
PasswordAuthentication {{SSHD_PERMIT_PASSWORD_AUTH}}
PubkeyAuthentication yes
PermitEmptyPasswords no
ChallengeResponseAuthentication no
KbdInteractiveAuthentication no
UsePAM yes
MaxAuthTries {{SSHD_MAX_AUTH_TRIES}}
LoginGraceTime 30
MaxSessions 6
MaxStartups 10:30:60

# --- Sane defaults ---
X11Forwarding no
AllowAgentForwarding no
AllowTcpForwarding yes
PrintMotd no
PrintLastLog yes
TCPKeepAlive yes
ClientAliveInterval 60
ClientAliveCountMax 3

# --- Logging ---
SyslogFacility AUTH
LogLevel INFO

# --- Strong crypto only (overrides Ubuntu defaults) ---
HostKeyAlgorithms ssh-ed25519,rsa-sha2-512,rsa-sha2-256,ecdsa-sha2-nistp521,ecdsa-sha2-nistp384,ecdsa-sha2-nistp256
KexAlgorithms sntrup761x25519-sha512@openssh.com,curve25519-sha256,curve25519-sha256@libssh.org,diffie-hellman-group16-sha512,diffie-hellman-group18-sha512
Ciphers chacha20-poly1305@openssh.com,aes256-gcm@openssh.com,aes128-gcm@openssh.com,aes256-ctr,aes192-ctr,aes128-ctr
MACs hmac-sha2-512-etm@openssh.com,hmac-sha2-256-etm@openssh.com,umac-128-etm@openssh.com

# --- SFTP via internal subsystem ---
Subsystem sftp /usr/lib/openssh/sftp-server